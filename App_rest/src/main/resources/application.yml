server:
  port: 8082

spring:
  application:
    name: rest-app

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://127.0.0.1:8885/eureka/
    instance:
      hostname: localhost


